{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "head": {
      "type": "object",
      "title": "head",
      "properties": {
        "color": { "$ref": "#/definitions/color"},
        "shape": {"$ref": "#/definitions/float"},
        "eyes": {
          "type": "object",
          "properties": {
            "position": {"$ref": "#/definitions/float"},
            "width": {"$ref": "#/definitions/float"},
            "distanceBetweenEyes": {"$ref": "#/definitions/float"},
            "count": { "$ref": "#/definitions/float"}
          },
          "required": ["position", "width", "distanceBetweenEyes", "count"]
        }
      },
      "required": ["color", "shape", "eyes"]
    }
  },
  "required": ["head"],
  "definitions": {
    "float": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "color": {
      "type": "object",
      "properties": {
        "h": { "$ref": "#/definitions/float"},
        "s": { "$ref": "#/definitions/float"},
        "l": { "$ref": "#/definitions/float"}
      },
      "required": ["h",  "s", "l"]
    }
  }
}
