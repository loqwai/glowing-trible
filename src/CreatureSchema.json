{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "head": {
      "type": "object",
      "title": "head",
      "properties": {
        "color": { "$ref": "#/definitions/color"},
        "shape": {"$ref": "#/definitions/float"},
        "eyeContainer": {
          "type": "object",
          "properties": {
            "position": {"$ref": "#/definitions/float"},
            "width": {"$ref": "#/definitions/float"},
            "distanceBetweenEyes": {"$ref": "#/definitions/float"},
            "eyeCount": { "$ref": "#/definitions/float"}
          },
          "required": ["position", "width", "distanceBetweenEyes", "eyeCount"]
        }
      },
      "required": ["color", "shape", "eyeContainer"]
    }
  },
  "required": ["head"],
  "definitions": {
    "float": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "color": {
      "type": "object",
      "properties": {
        "h": { "$ref": "#/definitions/float"},
        "s": { "$ref": "#/definitions/float"},
        "l": { "$ref": "#/definitions/float"}
      },
      "required": ["h",  "s", "l"]
    }
  }
}
